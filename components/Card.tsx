interface CardProps {
    name: string;
    title: string;
    department: string;
    image: string;
}

function Card({ name, title, department, image }: CardProps) {
    return ( 
        <div>
            
        </div>
     );
}

export default Card;